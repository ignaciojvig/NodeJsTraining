import { QueryParamToNumberPipe } from './query-param-to-number.pipe';

describe('QueryParamToNumberPipe', () => {
  it('should be defined', () => {
    expect(new QueryParamToNumberPipe()).toBeDefined();
  });
});
