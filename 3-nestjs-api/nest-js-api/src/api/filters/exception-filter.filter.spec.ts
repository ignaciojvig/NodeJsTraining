import { ExceptionFilterFilter } from './exception-filter.filter';

describe('ExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionFilterFilter()).toBeDefined();
  });
});
